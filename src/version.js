// App version shown in the UI. Auto-bumped (patch) on every commit by the
// pre-commit hook (.githooks/pre-commit -> scripts/bump-version.mjs).
// Edit the major/minor here by hand when you want to roll those.
export const APP_VERSION = '2.1.66';
